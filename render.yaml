services:
  - type: web
    name: castlekeep-iss
    env: node
    region: us-east-1
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
